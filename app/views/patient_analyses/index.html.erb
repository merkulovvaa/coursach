 <div class="container" style="margin-top: 10vh;">
<div id="patient_analysis">
<div class="row">
  <% @patient_analyses.each do |patient_analysis| %>
    <div class="col-md-4">
      <%= render patient_analysis %>
    </div>
  <% end %>
</div>
</div>
<%= paginate @patient_analyses, class: "custom-pagination" %>