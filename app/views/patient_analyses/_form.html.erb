<%= form_with(model: patient_analysis) do |form| %>
  <% if patient_analysis.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(patient_analysis.errors.count, "error") %> prohibited this patient_analysis from being saved:</h2>

      <ul>
        <% patient_analysis.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :analysis_id,"Analysis", style: "display: block" %>
    <%= form.collection_select :analysis_id, Analysis.all, :id, :name_analysis %>
  </div>

  <div>
    <%= form.label :analysis_date, style: "display: block" %>
    <%= form.date_field :analysis_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
